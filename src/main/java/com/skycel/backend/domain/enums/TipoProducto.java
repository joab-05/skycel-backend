package com.skycel.backend.domain.enums;

/** Guardado como TINYINT (ordinal): agregar valores nuevos siempre al final, nunca reordenar ni insertar a la mitad. */
public enum TipoProducto {
    CELULAR,
    ACCESORIO,
    SERVICIO,
    TABLET
}